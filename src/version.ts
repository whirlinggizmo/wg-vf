// The framework package version (semver). Keep in sync with package.json's
// "version" on release. Exposed for diagnostics; see the versioning section of
// the vignette author guide for the full model (wire / ABI / vignette / package).
export const VERSION = '0.1.0';
